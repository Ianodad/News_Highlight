class Sources:

    '''
    Class for holding news api
    '''

    def __init__(self, id, name, descript, url, cat):

        self. id = id
        self.name = name
        self. descript = descript
        self.url = url
        self.cat = cat
